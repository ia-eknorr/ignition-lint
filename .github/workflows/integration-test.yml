name: Integration Test

on:
  push:

jobs:
  lint-test-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Self test
        uses: ia-eknorr/ignition-lint@initial-setup
        with:
          files: ./tests/integration/view.json
